import { BadgeCustomPipe } from './badge-custom.pipe';

describe('BadgeCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new BadgeCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
